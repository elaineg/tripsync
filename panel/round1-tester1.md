NAME: Priya

```json
{"name":"Priya","clarity":"Yes","value":"Marginal","advocacy":8,"view_only_clear":"Yes","complaints":["Parse re-maps weekday labels to today's actual dates (my 'Friday/Saturday/Sunday' itinerary became Mon Jun15/Tue Jun16/Wed Jun17) with no date picker in the paste flow to correct it","No confirmation toast after copying a link beyond the inline 'Copied ✓'; I network-tab-checked to be sure the /v/ link was real and distinct from /t/"]}
```

Loaded cold. Headline "Turn a messy itinerary into a shared day-by-day calendar — no app, no login" plus the "No account or email required" footer told me exactly what it is and that it respects my no-signup stance in <10s. I'd tell a friend: "paste your group-chat plan, get a phone-friendly calendar link, no install."

The two share links are genuinely discoverable and unmistakable. The TRIP DETAILS panel shows them stacked: "Edit link — anyone can edit / Anyone with this link can view AND edit" (Copy invite link) and "View-only link — read-only / They can see the plan and add it to their own calendar, but can't change anything" (Copy), with a one-liner "Edit = companions who plan with you · View-only = anyone you just want to show." I would never confuse which to send. Links are structurally distinct: /t/<id> for edit, /v/<id> for view. Clipboard copy worked (label flips to "Copied ✓"; I verified the actual /v/ URL via clipboard read).

View-only is real read-only, not fake-disabled. Opening /v/ in a clean context: yellow banner "View-only — you can't edit this trip. Ask the trip owner for the edit link to make changes." The edit affordances are REMOVED, not greyed/dead — no "Your name", no "Copy invite", no "Paste itinerary", no "Save to .ics" button; instead a clean "Download .ics" link. Clicking an event gives "bonfire / Proposed by the organizer / Add to Google Calendar / Close" — so a relative can still drop it into their own calendar. Exactly what I want: look-but-don't-touch that's still useful to the recipient. No console errors anywhere.

Edit link, opened fresh, still has full power (Confirm/Edit/Delete/rename, paste, invite copy) — no regression.

Value is Marginal-to-Yes for ME personally: today I'd just paste into the group chat or a shared Google Cal, which I already have. This is genuinely better for the non-technical-relatives case (no app, phone-friendly, read-only). It's a real recurring pattern but not weekly for me, so 8 not 9: the weekday→wrong-date remapping in parsing is the one thing that made me distrust it for a beat, and there's no date control in the paste step to fix it.
