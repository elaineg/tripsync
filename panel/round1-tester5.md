# Dana — round 1

**1. CLARITY — Yes.** One scroll told me everything: "Turn a messy itinerary into a shared day-by-day calendar — no app, no login" plus "Add events to Google Calendar or download as .ics." Two clear cards (Paste an itinerary / Start from a blank calendar) and "No account or email required." I knew exactly what it was in under 10 seconds.

**2. VALUE — Yes (with a caveat).** Today I'd build the itinerary in Notion and nag everyone to add events manually. Here, paste→parse correctly turned my doc into 12 timed events with an editable preview before confirming, and every event has a clear "Add to Google Calendar" button — that solves my "nobody misses the brunch reservation" problem directly. The .ics export is a bonus. I'd use this for the weekend. Caveat: it makes the plan *functional*, not *beautiful*.

**3. ADVOCACY — 7.** Solid, I'd send it to a friend organizing a trip — but I'm holding it back from 8+ because the calendar is a flat-purple Google-Calendar clone with zero personality: no cover image, no per-day or per-category color, no vibe. I made a Canva mood board for this weekend; this looks like a work calendar next to it. It nails the job, not the joy. (A 7 by my own rule = a real, nameable blocker, and the blocker is visual polish.)

**Biggest blocker:** Calendar visual polish — single flat purple blocks, no theming/cover/color-coding. Functional but not screenshot-worthy for the team channel.

**Management-feature notes:**
- Recent trips list: clean, NOT cluttered. The two removal actions ARE distinguishable — "Remove from my list" is plain grey text (local), the red trash icon = "Delete trip for everyone…" with a confirm "This can't be undone." Good destructive-action hygiene. Nit: pencil/trash icons are tiny and icon-only on desktop.
- Inline rename (row → text field + check/X) is genuinely polished, in-place, no modal. Nice.
- Trip page: "..." = "Trip options" menu with pencil "Rename trip" + red "Delete trip" — clear, not junk. Header title is click-to-rename (good affordance with pencil). "Create New" button (home icon + pill) reads as nav, not header clutter.
- Mobile landing stacks cleanly and is tappable.

```json
{"tester": 5, "round": 1, "clarity": "Yes", "value": "Yes", "advocacy": 7, "topComplaints": ["Calendar is flat-purple Google-Calendar clone, no Canva-grade theming/cover/per-day color", "Recent-list rename/delete icons are tiny icon-only on desktop"], "priorConcernsAddressed": "n/a"}
```
