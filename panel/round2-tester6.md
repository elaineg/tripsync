NAME: Jules

{"name":"Jules","clarity":"Yes","value":"Yes","advocacy":9,"view_only_clear":"Yes","prior_concern_addressed":"Partial","complaints":["Share links still copyable before I hit 'Add to Camping trip' (out-of-scope this round) — a hasty Jules can still send an empty trip","My one-line 'Friday 5pm Leave town' still rejected, needs a day-header (out-of-scope this round)"]}

PRIOR CONCERNS (my two round-1 holdbacks): both were flagged out-of-scope for this view-only round, so I expected them unchanged. (1) share-before-confirm: STILL present — the Copy buttons sit live in the share bar while the parse is in the "Skip / Parse →" preview state. (2) one-line format: STILL rejected — "Friday 5pm Leave town" returns "Couldn't find any timed events. Try lines like…"; I had to use a day-header block. Neither was promised this round.

VIEW-ONLY FIXES (what WAS shipped) — all four verified, clean:
- Two links discoverable & distinct, now ABOVE Trip Details: measured Edit link y=127, View-only link y=169, TRIP DETAILS y=264. "Copy view-only link" is parallel to the edit/invite copy and clearly labeled "View-only link — read-only · they can add it to their own calendar but can't change anything." Perfect for my crew split.
- Genuinely read-only: opened /v/… in a fresh tab — banner "View-only — you can't edit this trip. Ask the trip owner for the edit link," 0 edit buttons, NO leaked edit hint (no "drag the grid" text), event card shows Add-to-Google only, no Save/Delete.
- Opens on the FIRST event day (Aug 7), not today — confirmed, Aug 7 tab active.
- Personal export still works in view-only (Download .ics + Add to Google Calendar). Edit link still full-edits.

CLARITY — Yes. Same crisp headline + "no app, no login" + footer "No account or email required." Instant pitch for a no-login marketer.

VALUE — Yes, recurring. Replaces dropping a plan in Notion/group chat that nobody can act on. One link, lands friends on the real dates, they push to their own calendar. The edit-vs-view split now makes it safe to share widely — that's the upgrade that pushes me past round 1.

ADVOCACY — 9 (was 8). The view-only feature is exactly what I'd have asked for and it's spotless, so I'm raising it. Not a 10 only because the share-before-confirm trap (I literally sent an empty trip in round 1) is still live and is the kind of thing that burns a first-time sharer; the moment a "hold the link until events are committed" guard ships, this is a 10 I'd post about unprompted.
