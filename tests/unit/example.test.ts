// Pattern for unit tests: import pure logic from lib/ and assert on it.
// Replace with real tests for this app's core logic.
import { describe, it, expect } from 'vitest';

describe('template sanity', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
